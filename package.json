{
  "name": "ajk-iconfonts-lib",
  "version": "0.1.3",
  "description": "ajk-iconfont-lib",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/babel-cli/bin/babel.js src --out-dir build",
    "watch": "./node_modules/babel-cli/bin/babel.js src --watch --out-dir build",
    "copyzip": "cp -r zipHtml package.json build",
    "release": "npm run copyzip && (cd build && npm publish)",
    "patch": "npm version patch && npm run release"
  },
  "author": "rockywu <wjl19890427@hotmail.com>",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-iconfont": "^8.0.1",
    "gulp-rename": "^1.2.2"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.9.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
